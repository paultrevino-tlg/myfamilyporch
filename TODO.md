# My Family Porch — build plan

Work top to bottom. Check items off as you go. Each phase has a **→ read** line:
open those `docs/SPEC.md` sections and prototypes before building, not the whole
spec. Each phase should leave the app building and deployable.

## Phase 0 — Project setup
→ read: `docs/ARCHITECTURE.md`
- [x] 0.1 `npm install`; if any package fails to resolve, install latest and update package.json
- [x] 0.2 Create/link the Supabase project; set env in `.env.local` + `.dev.vars`
- [x] 0.3 Apply `supabase/migrations/0001_init.sql` (Supabase MCP or `npm run db:push`)
- [x] 0.4 `npm run db:types`
- [x] 0.5 `npm run seed:prompts` (bilingual global library)
- [x] 0.6 Confirm `npm run preview` runs on the Cloudflare runtime
- [x] 0.7 Connect repo to Cloudflare for auto-deploy — verified live at myfamilyporch.paul-trevino.workers.dev (build cmd `npx opennextjs-cloudflare build`, deploy from `main`)

## Phase 1 — Auth, tenancy, membership
→ read: SPEC § Multi-tenancy & roles · `docs/ARCHITECTURE.md`
- [x] 1.1 Supabase Auth (passwordless/email) for family members — magic-link via Supabase Send Email hook → EmailJS (branded template); verified end-to-end (link → /dashboard → sign-out). Deliverability hardening (authenticated sending domain) tracked separately.
- [x] 1.2 `create_family` onboarding (caller becomes owner) — no-family members are routed to `/onboarding`; name form calls the `create_family` RPC (security-definer, makes caller owner) via a server action, then lands on `/dashboard`
- [x] 1.3 Family switcher; invitations + accept; roles (owner/admin/viewer) — active-family cookie + switcher; admins invite by email (EmailJS) → `/invite/[token]` accept via `accept_invitation` security-definer RPC (migration `0002`); login/callback honor a safe `?next`
- [x] 1.4 Verify RLS: a member of family A cannot read family B — `supabase/tests/rls_tenancy.test.sql` (impersonates authenticated users via JWT claims; asserts cross-family read+write isolation; runs in BEGIN…ROLLBACK). Passed against the linked project; a negative-control run confirms the assertions actually fire.

## Phase 2 — Storyteller surface
→ read: SPEC § Storyteller onboarding & auth, Storyteller session flow, Localization, Elder-facing UX · prototype `docs/prototypes/storyteller-flow.html`
- [x] 2.1 Storyteller + relationship CRUD (name, pronouns, birth year, language, address term) — admin-gated server actions (`storytellers/actions.ts`) + management page (`/storytellers`); storyteller holds shared facts, the per-member relationship holds address term/kind/asker_relation/interviewer (filtered to the signed-in member); dashboard links to it. RLS-conformant (admin writes), no schema change.
- [x] 2.2 Magic-link token mint/validate (HMAC, revocable) at `/s/[token]` — `lib/storyteller/token.ts` (Web Crypto HMAC; store only the hash; fail-closed validate; revoke). `/s/[token]` validates and welcomes (or shows a calm dead-link screen); `api/storyteller/answer` gates on the token. Admin mint/revoke buttons on `/storytellers` (raw URL shown once). Round-trip incl. tamper + revoke verified against the DB. Needs `STORYTELLER_TOKEN_SECRET` as a Cloudflare Worker secret for live use.
- [x] 2.3 Seven-screen flow (notification→welcome→question→answer→follow-up→answer→done) — client state machine `s/[token]/SessionFlow.tsx` (Welcome→Question→Your turn→AI follow-up→Your turn→Done; the prototype's screen-0 notification is the external SMS nudge, Phase 4.3). Elder-facing: one full-screen step, one big single-tap action, forgiving exits ("Maybe later"/"Skip this one"), never dead-ends. New `(storyteller)/layout.tsx` loads Atkinson Hyperlegible + Fraunces (scoped to elder surface). Bilingual via `lib/i18n` (en/es). Clearly-marked seams: mic 2.4, capture/write 2.5, real question+follow-up 3.1/3.2, cloned voice 4.2, `{address}` resolution 3.1. Build/type-check clean; live-verified welcome render EN+ES + soft dead-link against a minted token.
- [x] 2.4 Mic priming + denial recovery; emit mic-failed signal + SMS to admin — `SessionFlow` gains a **prime** step (friendly screen → `getUserMedia` on tap; grant stops tracks immediately, real capture is 2.5) and a calm **denied** recovery step ("open Settings" + Try again, never dead-ends). On denial the client beacons `api/storyteller/mic-failed` (new route): validates token, writes a `mic_failed` insight via service role (throttled — 6h window, skips if an undismissed one exists), and makes a best-effort admin SMS. Per master-arch messaging rule, the Twilio send is gated as a swallowed seam (transport 4.3, admin number 5.5). Bilingual (en/es). Surfacing on Overview is 6.2.
- [x] 2.5 Audio capture → private Storage; `api/storyteller/answer` writes via service role — `SessionFlow` records real audio on the two "Your turn" screens (MediaRecorder; codec via `isTypeSupported`, mp4 fallback for Safari) and uploads multipart to `api/storyteller/answer`. Route validates the token, then via service role lazily creates a `sessions` row, uploads to private bucket `story-audio/{family}/{storyteller}/{session}/{uuid}.{ext}` (migration `0003`, no RLS policies → service-role writes only; signed-URL playback is 5.2), and writes the `answers` row. Follow-up threads via `parent_answer_id`+reused `session_id`; `final` closes the session. Fail-soft: capture failure → mic recovery; skip/errors never strand. `transcript` null (STT 3.4), `question_text` placeholder (prompts 3.1/3.2). Verified live: primary+follow-up 200s, 401/400 negatives, DB scoping + parent link + session completion + 2 private objects, then cleaned up.
- [x] 2.6 i18n: render flow in storyteller language (en/es) — the flow already routed every visible string through `lib/i18n` `t()` keyed on the per-storyteller language; 2.6 completes it by setting the `lang` attribute (`<main lang={lang}>` on the live flow, `lang="en"` on the dead-link screen) so screen readers pronounce the en/es UI correctly, and by auditing the surface (3 files) for hardcoded strings — none leak. Verified live: an `es` storyteller renders `<main lang="es">` + Spanish copy (no English leaks), dead link renders `lang="en"`; type-check clean.

## Phase 3 — The AI interview loop
→ read: SPEC § The AI interview loop, Multi-generation relationship model
- [x] 3.1 Session assembly: resolve prompt tokens from storyteller + relationship — `lib/ai/assembly.ts` (server-only, service role) builds the `RelationshipContext` from the storyteller + interviewer relationship (prefers `is_interviewer`), picks a warm-up-first, not-already-asked prompt in the storyteller language, skips `{partner}`/`{asker_parent}` prompts (no data source yet → 3.3), and resolves tokens via `interviewer.resolveTokens`. `/s/[token]` assembles server-side and hands `address`/`question`/`promptId` to `SessionFlow` (greets by address term, shows the real opening question, sends `question_text`+`prompt_id` on the opening answer; null → placeholders, never dead-ends). `answer` route stores a UUID-validated `prompt_id`. Verified: type-check clean; selection query against the linked DB returns warm-ups first for a real storyteller; `resolveTokens` resolves `{address}/{name}/{they}/{them}/{their}` correctly for a `he_him` context.
- [x] 3.2 `api/ai/interview`: generate a natural follow-up from answer + coverage — `interviewer.generateFollowUp` makes one server-side Anthropic call (`claude-sonnet-4-6`, `ANTHROPIC_MODEL` override) with a prompt-cached system prompt encoding the SPEC philosophy (one question, follow the thread, speakable, storyteller language, EN/ES code-switching, never stack heavy, don't push on reluctance). `assembly.buildRelationshipContext` (extracted) resolves names/pronouns/era server-side from data, never the client. `api/ai/interview` validates a storyteller token, loads the scoped answer, computes light coverage gaps; transcript present → AI follow-up, else first unused pre-authored follow-up (token-resolved), else null — fail-soft, degrades to pre-authored if the Anthropic key is unset. `SessionFlow` fetches the follow-up after the opening answer saves and records it as that answer's `question_text`. Verified: type-check clean; live `generateFollowUp` EN+ES (warm single questions, correct language); route over HTTP (preauthored + ai sources, 401 bad token, 404 tenant scoping); test data + temp scripts cleaned up. AI path activates once STT (3.4) fills transcripts. **Prod:** set the Cloudflare `ANTHROPIC_API_KEY` secret before 3.4 (route degrades to pre-authored until then).
- [x] 3.3 Topic weighting + `applies_to` gating + emotional-weight pacing/avoid rules — `assembly.selectOpeningPrompt` (pure, deterministic) now gates the opening question by `applies_to` (prompt must fit the interviewer `kind` or be `any`), paces by emotional weight (heavy locked until 3 completed sessions; re-blocked right after a heavy answer so two never stack; warm-ups lead the first 2 sessions), weights topics toward the least-covered category, and honors an `avoidCategories` filter (Topics-steering seam, wired in 5.3). Signals derived server-side via service role: completed-session count, per-category answer counts + most-recent answer weight (one `answers→prompts` join). No schema change; `api/ai/interview` keeps its lighter coverage hint (the never-stack-heavy rule already lives in the 3.2 system prompt). Verified: type-check clean; pure-selector harness over the EN seed proves every rule (gating null for a mismatched `kind`, heavy gate at 2 vs 3 sessions, never-stack-heavy, least-covered wins, avoid filter); full live path against a real grandparent storyteller opens with a warm-up and shifts categories under an avoid filter.
- [x] 3.4 STT transcription (incl. code-switching tolerance) — `lib/voice/stt.ts` (`transcribe`, server-only) calls Deepgram nova-3 with `language=multi` (smart_format+punctuate), purpose-built for EN↔ES code-switching: one transcript, both languages interleaved, never translated/corrected. Wired into `api/storyteller/answer` — upload + STT run concurrently (no added latency) and fail-soft (any miss → transcript null, answer still saves, follow-up degrades to pre-authored). Transcribes every answer (opener/follow-up/final) for Stories review + the keepsake, and because it's synchronous before the response, the transcript is present when SessionFlow calls `api/ai/interview`, so the 3.2 AI follow-up path now activates. No schema/interview-route/client change. Verified: type-check clean; live `transcribe()` on EN, ES, and an EN↔ES code-switch clip (one mixed transcript); full route e2e against the real grandparent storyteller — posted a code-switch clip, DB confirmed the transcript persisted, then `api/ai/interview` returned `source:"ai"` with a thread-chasing follow-up ("What was the hardest part about that move for you, Grandpa Tony?"); test data cleaned up. **Prod:** `DEEPGRAM_API_KEY` set as a Cloudflare Worker secret (runtime; route degrades to pre-authored without it).

## Phase 4 — Voice & messaging
→ read: SPEC § Voice, Localization
- [x] 4.1 Voice profile setup (ElevenLabs clone) per interviewer; multilingual TTS — `lib/voice/elevenlabs.ts` (server-only): `cloneVoice` (Instant Voice Cloning), `synthesize` (real TTS via `eleven_multilingual_v2` — one cloned voice speaks both en+es), `deleteVoice`. `api/voice/clone` (admin+RLS gated) records the interviewer's in-browser sample (`VoiceSetup.tsx`, reuses the storyteller MediaRecorder pattern), sends it straight to ElevenLabs (no audio persisted — only the returned `voice_id`), inserts `voice_profiles`, and links `storyteller_relationships.voice_profile_id`; cleans up on failure/re-clone. `api/voice/preview` proves multilingual playback; `deleteVoiceProfile` action removes it. Wired into `/storytellers` interviewer section with en/es preview. Verified: type-check clean; live ElevenLabs round-trip (creator tier, IVC enabled) — cloned a voice, synthesized EN+ES mp3 from the same voice, deleted it; lib fails closed without a key; both routes return 403 unauthenticated. **Prod:** `ELEVENLABS_API_KEY` set as a Cloudflare Worker secret. Cloned-voice playback on the storyteller question screen is 4.2.
- [x] 4.2 Question playback in the interviewer's cloned voice — token-gated `api/storyteller/voice` validates the magic-link token (service role), resolves the interviewer's cloned voice (`storyteller_relationships` → prefer `is_interviewer` → `voice_profiles.provider_voice`), and returns `audio/mpeg` via 4.1's `synthesize`. Fail-soft: 204 (no voice linked), 502 (synth fail), 401 (bad token) → client shows text only. `SessionFlow` gains a `QuestionVoice` component on the question + follow-up screens: fetches audio on screen entry, best-effort autoplay + a big tap-to-(re)play chip; large question text stays as the backup channel and never blocks progress (SPEC § Voice). 2 i18n keys (en/es). No schema change. Verified: type-check clean; live route e2e (real cloned voice temporarily linked) — EN+ES return valid playable mp3 (200 audio/mpeg), bad token 401, empty text 400, no-voice 204; all test data cleaned up. Note: iOS autoplay is gesture-restricted, so on-device the elder may tap the chip (verified bytes + fallbacks here). `ELEVENLABS_API_KEY` already on Cloudflare (4.1).
- [ ] 4.3 Twilio SMS nudges (per-storyteller, localized) + deep link — **CODE DONE & COMMITTED; NOT marked done — blocked on Twilio A2P 10DLC registration for real delivery. Check off only after a live SMS is confirmed delivered.**
  - **Code (committed, type-check clean):** `lib/sms/twilio.ts` real `sendSms` (Worker fetch + Basic auth, fail-soft on missing env, un-stubs the 2.4 mic-failed alert seam); `lib/sms/nudge.ts` new — `buildNudge(lang,{address,interviewer,url})` (localized line + deep link on its own line, drops "it's {interviewer}" when no name) and `sendStorytellerNudge(storytellerId,familyId)` (resolves phone/lang/address, interviewer name via auth metadata→asker_relation→drop, reuses newest active token or mints one → `${APP_BASE_URL}/s/<token>`, fail-soft no-phone/no-link); `lib/i18n` `sms_nudge`/`sms_nudge_no_interviewer` (en+es); `/storytellers` admin-gated `sendNudge` action + "Send a nudge" button (shown when phone on file) + result flashes (added `phone` to the page query).
  - **Tested up to the carrier boundary:** `buildNudge` en/es/no-interviewer correct; Twilio creds valid (acct active); real `sendSms` to +14806784044 was **accepted/queued** by Twilio — but delivery returned `status=undelivered, error_code=30034` (US A2P 10DLC: unregistered sending number). Not a code bug.
  - **Cloudflare prod secrets SET** (via `wrangler secret put --name myfamilyporch`): `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` (= `+16232358221`). All other secrets incl. `APP_BASE_URL` were already set — left untouched (local `.dev.vars` APP_BASE_URL is `localhost`; do NOT push that to prod). **Auth Token rotated in Twilio (2026-06-04) after being pasted in chat — the old token is now invalid.**
  - **Campaign rejected once (2026-07-06):** "invalid sample message content" (samples lacked opt-out language + variable fields weren't bracketed; `/s/abc123` read as a link shortener) and "issues verifying the CTA" (proxy consent — the family member consented for the recipient, carriers require the recipient's own consent).
  - **Fix shipped (2026-07-06): double opt-in.** Migration `0014_sms_consent.sql` (`storytellers.sms_consent` pending/confirmed/stopped + `sms_confirm_sent_at`); `api/sms/inbound` webhook (Twilio-signature-validated via Web Crypto, YES/START/SÍ→confirmed, STOP-family→stopped, HELP/AYUDA→program reply; matches storyteller by phone digits, applies across families); `sendStorytellerNudge` consent gate — pending → sends the one-time "reply YES" confirmation (24h re-send throttle) instead of a reminder, stopped → skip; every nudge now ends with "Reply STOP to opt out" (en/es); phone change in Settings resets consent to pending; new dashboard banners for confirm-sent / awaiting / stopped; `/sms` page rewritten (two-step consent, confirmation message shown, samples bracketed `[Name]`/`[unique-code]` + STOP line, frequency "up to 1 msg/day"); consent checkbox copy updated to mention the YES step.
  - **Campaign rejected twice (2026-07-08, error 30909):** CTA still unverifiable — the point of collection (phone field + consent checkbox) is behind login, and per Twilio's 30909 remediation a non-public flow needs a public URL with hosted screenshots.
  - **Fix shipped (2026-07-08): consent-flow screenshots on `/sms`.** New "What the sign-up form looks like" section hosting real dashboard screenshots (sample data: "Rosa Martinez") — `public/sms/opt-in-form.png` (phone field + checked consent attestation) and `public/sms/awaiting-yes.png` (amber waiting-for-YES banner). Retake both if the form copy or checkbox wording ever changes (captured via Playwright against local dev with a seeded temp user, then cleaned up).
  - **RESUME HERE — resubmit the campaign after deploy:** (1) In Twilio Console → the number `+1 623 235 8221` → Messaging Configuration → set "A message comes in" webhook to `https://myfamilyporch.net/api/sms/inbound` (HTTP POST). (2) Edit + resubmit the rejected campaign with: samples = the two bracketed messages on `/sms` (incl. the "Reply STOP to opt out" line); opt-in message = the confirmation SMS from `/sms`; opt-in keywords = `YES, START, SÍ`; opt-out `STOP` + help `HELP` with their reply messages; message-flow/CTA description = the full end-to-end two-step flow (family admin enters number + attestation checkbox behind login → recipient must reply YES to the confirmation text before any reminders send), explicitly naming `https://myfamilyporch.net/sms` as the public opt-in page **with hosted screenshots of the login-protected form**, plus privacy `https://myfamilyporch.net/privacy` + terms `https://myfamilyporch.net/terms`, frequency "up to 1 msg/day", and "Message and data rates may apply"; CTA URL = `https://myfamilyporch.net/sms`. (3) After approval, live-test: save a phone → "Send a nudge" → expect the confirmation SMS → reply YES → nudge delivers. Verified destination for testing: `+14806784044`.

## Phase 4C — Consent-flow redesign (docs/consent-flow.md)
→ read: `docs/consent-flow.md` · `PLAN-consent-flow.md` · master-arch § SMS via Twilio / A2P 10DLC
Replaces the proxy-consent "reply YES" model (4.3 / migration `0014`) — rejected twice
(error 30909) for proxy consent + A2P-to-unconsented-number. New spine: storyteller's
first contact is P2P; consent is their own first-person tap on an authorization page;
append-only `consent_events` audit + global `sms_suppressions` + bilingual keyword/NL
opt-out. **Decisions (2026-07-13):** full member A2P · rename to `consent_state` ·
full replace (no flag) · build a real setup wizard. Each task = its own two-gate step.
- [x] 4C.A Schema & audit spine — migration `0015_consent_flow.sql`: `consent_events`, `sms_suppressions`, `sms_inbound`; rename storyteller `sms_consent`→`consent_state` (pending/opted_in/opted_out, values remapped 1:1); add member `consent_state`/`sms_phone`/`language`/`sms_confirm_sent_at` on `memberships`; `consent_events.language`. RLS: `ce_select`=`is_member_of` (append-only, service-role writes); suppressions/inbound RLS-on-no-policies (service-role only). Types regenerated; mechanical rename (behavior-preserving) in `nudge.ts`/`api/sms/inbound`/`settings/actions.ts`. Verified: `tsc` + `npm run build` clean, no `sms_consent` left in `src`, DB confirms columns/tables/RLS.
- [x] 4C.B Member phone verify + SMS opt-in — **email stays the account identity; SMS is a possession + opt-in channel** (confirmed mechanism). `lib/consent/token.ts` shared signed-token primitive (HMAC over JSON payload, purpose `member_verify`|`consent`, stateless, fail-closed, reuses `STORYTELLER_TOKEN_SECRET`); `lib/consent/member.ts` `startMemberVerification` (sets `sms_phone`+`pending`+`language`, mints token, sends `magic_link_sms` to the member's OWN number — fail-soft) + `confirmMemberVerification` (verify token → `opted_in` + append `consent_events(subject_type=member, method=magic_link)`, idempotent; disclosure stored as the English app-form copy, `// TODO` localize when app UI is bilingual). Pages: `(app)/verify-phone` (session-gated form: number + language + opt-in disclosure checkbox) + top-level token-gated `/verify/[token]` confirm (no session; calm dead-link on bad token). Entry: onboarding → `/verify-phone`; dashboard `VerifyPhoneBanner` until opted_in. Shared `lib/phone.ts` `normalizePhone` (settings refactored onto it). 6 i18n keys (en+es). No migration (Phase A columns), no new secret. Verified: `tsc` + `npm run build` clean (`/verify-phone`, `/verify/[token]` present); live 16/16 vs the linked DB (token sign/verify + tamper/expiry/purpose/no-secret; start sets phone+pending+language fail-soft; confirm → opted_in + exactly one consent_event, idempotent, tampered→invalid) then DB restored to original + temp script removed.
- [x] 4C.C Consent token + storyteller authorization page — `lib/consent/storyteller.ts`: `buildConsentLink` (mints `purpose='consent'` token → `/c/<token>`; consumed by 4C.D), `loadConsentContext` (paints name + last-4 + language), `confirmStorytellerConsent` (verify → `opted_in` + persist name/language edits + **clear any prior `sms_suppressions` for the number** (fresh first-party opt-in beats a past STOP) + append `consent_events(storyteller, opt_in, auth_page, exact localized disclosure, ip, ua)`, then fire step-9 storyteller confirm + step-10 family "ready"; idempotent). Step-10 gated on the interviewer member being `opted_in` + not suppressed, sent in the member's language; both sends fail-soft. `lib/sms/suppression.ts` `isSuppressed` (minimal pre-send gate; 4C.F generalizes). Elder-accessible bilingual page `(storyteller)/c/[token]` (Atkinson/Fraunces; ≥60px targets, ≥20/24px type; one-sentence what-it-is + what's-next; editable name + masked last-4; first-person opt-in **unchecked by default**; `?lang` switch; Terms/Privacy; `noindex`; calm dead-link + success) + `HearThis` client island (browser SpeechSynthesis read-aloud — no TTS infra/secret, works without a cloned voice) + token-gated submit action. 17 i18n keys (en+es), incl. step-9/10 SMS copy. No migration, no new secret (reuses `STORYTELLER_TOKEN_SECRET`/`APP_BASE_URL`). Verified: `tsc` + `npm run build` clean (`/c/[token]` present); live 13/13 vs the linked DB (token sign/verify/purpose; build link → context paints → confirm sets opted_in + name edit + exactly one auth_page event + suppression cleared + step-9/10 fired fail-soft; idempotent; bad token → invalid/null) then all seed data removed + membership restored + temp script deleted. **Reachable via a directly-minted link; the copy-paste-block UI to produce it is 4C.D.**
- [x] 4C.D Copy-paste P2P block + remove attestation checkbox — `setStorytellerPhone` drops the family-member consent gate (saving a number no longer records consent; the storyteller's own `/c/<token>` tap is the operative consent) + keeps the phone-change→`pending` reset. Storyteller hub: removed the attestation checkbox + its `error=consent` banner; added `consent_state` to the query; when a number is on file and not yet `opted_in`, mints a fresh link via `buildConsentLink` and renders the warm **copy-paste P2P block** (localized `copy_paste_block`, storyteller's language, name+link interpolated) + `CopyBlock` client island (clipboard) + plain "text it from your own phone" guidance; once `opted_in`, shows a calm "✓ opted in" status; **"Send a nudge" gated on `opted_in`** so the UI never triggers the old reply-YES path pre-consent (nudge.ts branch removed in 4C.F). Settings alert-phone checkbox (admin's OWN first-party consent) untouched. 1 i18n key (en+es). No migration/secret. Verified: `tsc` + `npm run build` clean (`/storytellers/[id]` present); grep confirms the proxy attestation is gone (only first-party alert/member checkboxes remain); live 5/5 (seed pending storyteller → buildConsentLink → loadConsentContext resolves name/last-4/lang → es message interpolates name+link, no leftover placeholders → seed removed), DB clean.
- [x] 4C.E Setup wizard + `setup-overview` graphic — `lib/setup.ts` pure `deriveSetupStep` (create_family→verify_number→add_storyteller→send_link→ready, derived from data each render — no stored wizard column) + `loadSetupState` (RLS-scoped; resolves the pending storyteller, preferring one with a number). `SetupOverview.tsx` = the SVG rebuilt as **responsive, localized HTML/CSS** (stacks vertically on mobile; pine=you 1-4 / slate=storyteller 5-6 / honey dashed handoff 4→5; header + legend + 🔔 notification callout; single `aria-label`, decorative emoji `aria-hidden` so meaning is number+text; no animation). `(app)/setup` orchestrator renders the graphic (current step highlighted) + a derived step card: verify→`/verify-phone?from=setup`, add→`/storytellers/new`, send→**inline copy-block** (reuses `buildConsentLink`+`CopyBlock`) + "waiting", ready→success→dashboard. Wiring: onboarding→`/setup`; `/verify-phone` threads `?from=setup` (Back/Skip/retry return to setup); dashboard banner → "Finish setting up" → `/setup`. ~30 i18n keys (en+es, overview labels verbatim from the SPEC table). No migration/secret. Verified: `tsc` + `npm run build` clean (`/setup` present); live 8/8 (pure step matrix 5/5 + seeded integration: pending→send_link, pending-with-phone preferred, cleanup), DB clean. **Responsive/visual is a manual check (no headless browser in the sandbox).**
- [x] 4C.F Inbound rewrite + universal pre-send gate — `lib/sms/keywords.ts` pure `classifyInbound` (bilingual EN+ES exact keywords + natural-language opt-out; normalize strips accents/case/punct; NL match → `needs_review`; `langHint`). `lib/sms/gate.ts` `preSendGate` (opted_in + not suppressed) — the one gate before every program send; reused by `nudge.ts` + step-10. **`api/sms/inbound` rewritten:** logs every inbound to `sms_inbound`; matches BOTH `storytellers.phone` and `memberships.sms_phone` (one number, many parties/families); opt-out → global `sms_suppressions` + every matched party `opted_out` + `consent_events(opt_out, sms_stop|natural_optout)` + one `stop_confirmation`; help → program reply, no state change; **START resubscribes ONLY if a prior STOP suppressed the number** (clear suppression + restore `opted_in` + `re_opt_in`), never a cold opt-in; reply language = matched-keyword lang → party lang → en. **`nudge.ts`:** dropped the "reply YES" confirmation branch entirely (removed `buildConfirmation`/`sms_confirm*`/`sms_confirmed_reply`), gates on `preSendGate`, and reconciles Twilio 21610 (carrier unsubscribe → mirror to suppressions + `opted_out`); `NudgeResult` reasons now `no-phone|no-link|not-opted-in|suppressed`; nudge actions + hub banners updated. i18n: added `sms_stop_confirmation`/`sms_start_welcome`, repurposed `sms_help_reply`, removed the dead reply-YES keys (en+es). No migration/secret. Verified: `tsc` + `npm run build` clean; live **21/21** vs the linked DB — classifier matrix (incl. accented `ALTÓ`, `no más mensajes`, `gracias`→none) + signed end-to-end route calls (STOP dual-party opt-out + suppression + events + inbound log; HELP no-change; START-after-STOP restore; START-not-suppressed → no cold opt-in + silent; natural-language opt-out + review flag), all seed data removed + member restored. **Manual (Twilio console):** disable Twilio's default STOP auto-reply (or use Advanced Opt-Out with our copy) so our webhook is the single responder — carried into 4C.G.
- [ ] 4C.G `/sms` rewrite + fresh screenshots + A2P campaign resubmit. **Launch gate:** TCPA attorney sign-off on flow + EN/ES disclosure wording.

## Phase 5 — Family/admin dashboard
→ read: SPEC § Admin dashboard surfaces · prototype `docs/prototypes/family-admin.html`
- [x] 5.1 Overview (status cards + recent stories) — `lib/overview.ts` (server-only, RLS-scoped, no service role) loads four status cards + a "Lately" list for the active family in parallel: **Last session** (most recent `sessions.completed_at`, ✓/green when within 7 days), **This week** (completed sessions in the trailing 7 days; "of N" only when a `schedules` row exists, N = summed `days_of_week`), **Stories saved** (top-level answers via head-count), **Topics touched** (distinct answered categories / 12 in the library). "Lately" = newest 5 top-level answers (question, category tag, storyteller, relative day, duration, follow-up count via the self-referential `answers!parent_answer_id(count)` embed). Rendered in `dashboard/page.tsx` above Storytellers with calm empty-states; `relDay`/`formatDuration` helpers + `Card`/`RecentRow` components. The 3 signal alerts are Phase 6; ▶ audio playback is 5.2. Verified: `tsc` + `next build` clean; seeded a marked dataset and confirmed every card's SQL math (stories=2, this-week=1, topics=2/12, no schedule → "of N" hidden) + the live PostgREST embed shape (storyteller/prompt objects, followups `[{count}]`, durations 181s→"3 min 1 sec"); seed removed.
  - [x] 5.1a Per-storyteller breakdown + detail pages — dashboard keeps the family-wide cards + "Lately", then renders one block per storyteller (`loadStorytellerStats` groups sessions/schedules/answers by `storyteller_id` in one pass) showing that elder's four metrics; the block links to a new `/storytellers/[id]` page (read-only config summary at top with Edit links out to /storytellers, /settings, /schedule, /topics; that storyteller's answers below via `loadStories(familyId, id)`). No schema change, RLS-bound reads only; cross-family id → 404. Verified: `tsc` + `next build` clean (`/storytellers/[id]` route present); per-storyteller SQL math matches the loaders (6+3 stories sum to the family total, this-week 2+2, last-session fresh).
- [x] 5.2 Stories review (audio + transcript + follow-up thread + edit + in-book) — `lib/stories.ts` `loadStories` (RLS-bound SSR client) loads top-level answers newest-first with category/storyteller/day/duration/transcript/`in_book` + the nested follow-up thread (self-referential `answers!parent_answer_id` embed, oldest-first). `/stories` page renders each story with a native `<audio>` player, transcript, threaded follow-ups (each with its own player), and — for admins — an *In the book* toggle + *Edit transcript* (`<details>` textarea, no client JS); viewers get read-only + an in-book badge. `api/stories/audio?answer=<id>` reads the answer via the SSR client (RLS `ans_select` → any role, family-scoped) then mints a 300s **signed URL** with the service role on `audio_path` and 307-redirects (400/401/404/502). `stories/actions.ts` admin-gated `toggleInBook`/`editTranscript` (RLS `ans_write` is the boundary). Dashboard Lately rows now play audio + link to `/stories` (added `hasAudio` to `overview.ts`). No schema change. **Deferred:** photo → 7.1 (needs schema); skip handling → later (no skip rows yet). Verified: `tsc` + `next build` clean; RLS `BEGIN…ROLLBACK` test PASSED (viewer reads + threading nests 1 follow-up, viewer can't edit, admin toggles in_book + edits transcript, cross-family member 404); storage signing live (anon denied, service-role signed URL → 200 exact bytes, tampered sig → 400), test object cleaned up.
  - [x] 5.2a Deletion cascade for media — `lib/storage/cleanup.ts` (server-only, service role): `removeAudioObjects(paths)` (dedupe/filter, `story-audio.remove`, **throws on error**), `collectAnswerAudioPaths(familyId, answerId)` (answer + follow-up thread), `collectStorytellerAudioPaths(familyId, storytellerId)` — both `family_id`-scoped so a forged id can't cross-tenant erase. Wired into `deleteStoryteller` (collect → remove → then delete row, so storage failure aborts before the cascade) and a new admin `deleteStory` action + a **Delete** disclosure per story on `/stories` (erases the answer, its follow-ups, and the recordings). Objects removed *before* rows, so a record is never deleted while its audio survives. **Deferred:** family-level erase hooks in when Settings 5.5 adds a family-delete flow (no UI today). No schema change. Verified: `tsc` + `build` clean; live against the real bucket + DB (seed → exercise → auto-cleanup) — all 10 checks PASSED (deleteStory erases only that story + its follow-up, sibling untouched, rows cascade; deleteStoryteller erases the whole archive + cascades rows; cross-family collect returns no paths). Note: confirm erasure via Storage `list()`, not `download()` (cached by `max-age=3600`).
- [x] 5.3 Topics steering (focus/ease-off/avoid) — migration `0005_topic_preferences.sql` (enum `topic_preference` + per-storyteller `topic_preferences` table, `unique(storyteller_id, category)`, RLS member-read/admin-write; applied to prod, types regenerated). Selector `lib/ai/assembly.ts` gains `focusCategories`/`easeOffCategories`: ranking is early-warmups → steering (focus −1 / neutral 0 / ease-off +1) → least-covered → warm-up tiebreak; `assembleOpeningQuestion` loads the storyteller's prefs server-side (avoid still the only hard exclude). `lib/topics.ts` `loadTopics` (per storyteller: each category's coverage + current pref). `/topics` page + `setTopicPreference` action (admin-gated upsert/delete; re-tap clears to neutral; viewers read-only) + dashboard Topics link. **Deferred:** custom-question authoring → own step. "Weight, not a script" — focus/ease-off only bias; the AI still chases what the elder opens. Verified: `tsc` + `build` clean; selector harness over the real 71-prompt EN library PASSED (focus to front, avoid excludes, ease-off sinks, focus>ease-off, avoid>focus, early warm-ups preserved, least-covered intact); RLS `BEGIN…ROLLBACK` PASSED (viewer reads not writes, admin CRUD, cross-family isolated).
- [x] 5.4 Schedule (days/time/quiet hours/pause/ask-now) — no schema change (the `schedules` table already fit). `lib/schedule.ts` `loadSchedules` (RLS SSR client) lists each storyteller with their schedule row or the table defaults; `/schedule` page renders an admin no-JS form per storyteller (Su–Sa day pills, time, questions-per 1–2, quiet hours, Pause) + a separate **Ask now**, viewers read-only. `schedule/actions.ts` admin-gated `saveSchedule` (validates family ownership, sanitizes inputs, upserts on `storyteller_id`) + `askNow` (reuses 4.3 `sendStorytellerNudge`). Dashboard gains a Schedule link. RLS (`sch_select`/`sch_write`) is the boundary. Verified: `tsc` + `build` clean (`/schedule` route present); RLS `BEGIN…ROLLBACK` test (`supabase/tests/rls_schedule.test.sql`) PASSED + negative control fired (admin upserts, viewer reads-not-writes, cross-family isolated); live data-shape confirmed. **Note:** Ask now shares the 4.3 Twilio A2P blocker for live delivery (fail-soft).
- [x] 5.5 Settings (numbers, voice, family access) — new `/settings` admin route matching the prototype's four fields. Migration `0006` adds nullable `memberships.alert_phone` (per-admin failure-alert number; applied to prod, types regenerated). `lib/settings.ts` RLS-scoped loader (storyteller phones, the signed-in admin's own alert number, cloned-voice status per interviewer relationship, family roster + invitations); member emails resolved via service-role lookup (auth.users isn't RLS-readable), scoped to the active family. `settings/actions.ts` admin-gated `setStorytellerPhone` (family-scoped) + `setAlertPhone` (scoped to the caller's OWN membership row); shared E.164 normalize, blank clears. `settings/page.tsx` four sections, admins edit / viewers read-only. `api/storyteller/mic-failed` now resolves every owner/admin `alert_phone` in the family and texts each (deduped, fail-soft) — replaces the `ADMIN_ALERT_TEST_NUMBER` stub. `createInvitation` returns to `/settings`; dashboard's family-access block becomes a link to Settings (one source of truth) + new Settings nav link. **Closes a standing gap:** storyteller phone was referenced by the nudge flow but never editable anywhere. Verified: `tsc` + `build` clean (`/settings` present); BEGIN…ROLLBACK prod tests — alert resolution returns owner+admin and excludes the viewer, storyteller phone set/clear round-trip. **Note:** live admin-alert SMS shares the 4.3 Twilio A2P blocker (fail-soft).

## Phase 6 — Signals & adaptive scheduling
→ read: SPEC § The three signals & adaptive scheduling · prototype toggle in `docs/prototypes/family-admin.html`
- [x] 6.1 Weekly cron Worker (`api/cron/scheduler`): send nudges — custom `worker.ts` entrypoint wraps OpenNext's generated worker (re-exports the DOs, reuses `handler.fetch`) and adds `scheduled()`, which fires one in-process self-call to the route with the `CRON_SECRET`; `wrangler.jsonc` `main`→`worker.ts` + hourly `triggers.crons`. The route is `CRON_SECRET`-gated (fail-closed) and runs `lib/scheduler/run.ts`: `runScheduler()` scans all non-paused schedules (service role) and sends the nudges **due now** via the pure, exported `isScheduleDue(row, now)` — scheduled-today / past send-time / before quiet-hours / not-already-nudged-today, all evaluated in the row's own **timezone**. Reuses 4.3 `sendStorytellerNudge` (fail-soft per row); stamps `last_nudged_at` only on a real send (misconfigured rows re-attempt + self-heal). Migration `0007` adds `schedules.timezone` (IANA) + `last_nudged_at` (applied to prod, types regenerated); the `/schedule` admin form gains a per-storyteller timezone picker (viewers read-only). **Verified:** `tsc` + `next build` clean; pure due-logic harness 12/12 (weekday gating, send-time `>=` boundary, timezone changes the verdict for the same instant, quiet-hours exclude, same-**local**-day dedupe incl. an in-zone day-boundary case, null-tz→default); live route vs the real DB — 401 without/with-wrong secret, 200 with correct secret returning `{considered:2, sent:[], skipped:[no-phone]}` (correctly selected the Wed-scheduled storyteller, not the Tue/Fri one), and no row mutated (stamp only on send). `CRON_SECRET` set as a Cloudflare Worker secret. **Note:** live SMS *delivery* still rides the 4.3 Twilio A2P blocker (fail-soft).
- [x] 6.2 Signal: mic-failed surfaced on Overview — `lib/overview.ts` `loadSignals(familyId)` reads undismissed `insights` for the active family via the RLS-bound SSR client (`ins_select` is the boundary, no service role), joined to storyteller name, newest first; typed over all 3 insight types so 6.3/6.4 reuse it. Dashboard renders an amber/⚠️ `SignalAlert` per mic-failed signal at the **top of Overview** (matching the prototype `.alert` tone + spec's status/3-signals/recent order): "{name}'s microphone didn't go through." + relative day/time. New `dashboard/actions.ts` `dismissInsight` (admin-gated like `stories/actions.ts`; sets `dismissed_at`, RLS `ins_write`=admin is the real guard, `family_id`-scoped) — Dismiss button for owners/admins only, viewers read-only. The matching admin SMS already shipped in 2.4/5.5; 6.2 is the surfacing half. No schema change. Verified: `npm run build` clean; `BEGIN…ROLLBACK` against the linked DB — loader query returned only the active insight (correct storyteller name + payload), excluded the dismissed row.
- [x] 6.3 Signal: schedule suggestion (different hour) + SMS, throttled — `lib/signals/schedule-suggestion.ts`: pure `deriveScheduleSuggestion` (baseline ≥4 of the most-recent 8 completed sessions → median engagement hour in the storyteller's **own timezone** → meaningful gap ≥2h vs the nudge → sustained: ≥60% cluster within ±1.5h) returns the best-time label + a half-hour-rounded suggested send time. `runScheduleSuggestions(now)` scans non-paused schedules (service role, pause-aware), reads each storyteller's recent completed-session times (`started_at ?? completed_at`), **throttles** on any `schedule_suggestion` insight within 30 days (so "Keep current time" isn't re-nagged, no pileup), inserts the insight, and texts the family via the new shared `lib/sms/admin-alert.ts` `alertFamilyAdmins` (mic-failed route refactored onto it). Wired into `api/cron/scheduler` after the nudges (`{nudges,suggestions}`). Dashboard renders a positive blue 💡 `ScheduleSuggestionCard` (prototype `#ins-well` tone, distinct from the rose mic alert) with **Switch to {time}** (`applyScheduleSuggestion` action — reads the suggested time off the insight, updates `schedules.send_time_local`, dismisses; "recommend, never auto-change" = explicit tap only) + **Keep current time** (reuses `dismissInsight`); viewers read-only. No schema change. Verified: `tsc` + `npm run build` clean; pure-fn harness 10/10 (cluster fires with 14:30, too-few/small-gap/scattered → null, timezone flips the verdict for the same UTC instants); live e2e vs the linked DB 11/11 (seeded afternoon sessions → one insight with correct payload, second run throttled/no pileup, cleanup confirmed 0 leftover + pre-existing sessions untouched). **Note:** family SMS rides the 4.3 Twilio A2P blocker (fail-soft); verified to the boundary only.
- [x] 6.4 Signal: engaging-less (sustained drop vs personal baseline) + gentle SMS, throttled — `lib/signals/engagement-drop.ts`: pure `deriveEngagementDrop` (recent 28d rate vs the elder's OWN prior-56d baseline; baseline gate ≥4 sessions, drop gate recent ≤50% of baseline; the multi-week recent window is the "sustained" measure) emits calm non-numeric labels ("about two a week"→"less than one a week"). `runEngagementDrop(now)` scans non-paused schedules (service role, pause-aware), throttles on any `engagement_drop` insight within 30 days, inserts the insight, and texts the family via the shared `alertFamilyAdmins` (gentle, non-clinical copy). Wired into `api/cron/scheduler` after the suggestions (`{nudges,suggestions,drops}`). Dashboard renders a calm slate 💬 `EngagementDropCard` (prototype `#ins-quiet`, distinct from rose mic + blue suggestion) with **Adjust schedule** (→ `/schedule`) + **Dismiss**; viewers read-only. No schema change (the `engagement_drop` enum + `Signal`/`loadSignals` were already generic). Sensitivity/on-off is 6.5. Verified: `tsc` + `npm run build` clean; pure-fn harness 8/8 (drop fires, steady/thin-baseline/75%/ancient → null, went-silent + 50%-boundary fire, garbage tolerated); live e2e vs the linked DB — seeded baseline-then-drop family, real `runEngagementDrop()` flagged only the test storyteller (real ones held the gates), one insight w/ correct payload (recent 0.5/wk, baseline 1.875/wk), second run throttled, cleanup confirmed DB back to original. **Note:** family SMS rides the 4.3 Twilio A2P blocker (fail-soft); verified to the boundary only.
- [x] 6.5 Sensitivity controls + pause-aware exclusions — migration `0008_signal_preferences.sql` (enum `engagement_sensitivity` gentle/standard/sensitive + three per-storyteller columns on `schedules`: `signal_engagement_enabled`, `signal_engagement_sensitivity`, `signal_schedule_suggestion_enabled`; applied to prod, types regenerated; no new RLS — columns inherit `sch_select`/`sch_write`). `deriveEngagementDrop` takes an optional `sensitivity` mapping to the drop threshold (gentle 0.35 / standard 0.5 / sensitive 0.65); `runEngagementDrop` skips `signal_engagement_enabled=false` rows and passes each row's sensitivity; `runScheduleSuggestions` skips `signal_schedule_suggestion_enabled=false` rows. `lib/schedule.ts` (`StorytellerSchedule` + both loaders) carries the fields; `saveSchedule` validates + persists them in the same upsert. Storyteller detail page gains a **Check-in alerts** subsection in the Schedule editor (two opt-out toggles + sensitivity select, plain-language help; viewers read-only). mic-failed stays always-on (acute). **Pause-aware already enforced** — all three runners scan `paused=false`; 6.5 adds an explicit end-to-end test. Verified: `tsc` + `npm run build` clean; pure-fn sensitivity harness 10/10 (moderate→sensitive only, mid→standard+sensitive, big→all three, default==standard); live e2e vs the linked DB 6/6 (engagement disabled→no flag, gentle mid-drop→no flag, sensitive→flag, **paused→no flag** despite qualifying data, suggestion disabled→not suggested, enabled+real hour gap→suggested), cleanup confirmed DB back to original.

## Phase 7 — Keepsake
→ read: SPEC § The keepsake book
- [x] 7.1 Book assembly: chapters by category, reorder, photos — migration `0009_book.sql` (applied to prod, types regenerated): additive `answers.book_sort` (float, drag-insert without renumber), `storytellers.book_chapter_order text[]`, new `story_photos` table (multiple photos/story; RLS sp_select=member / sp_write=admin) + a private `story-photos` bucket (service-role writes, signed-URL reads — mirrors `story-audio`). `lib/book.ts`: RLS-scoped `loadBook` (in-book answers grouped into chapters; pure `sortChapters` orders by the admin's saved order → canonical life-arc → unknown alphabetical; stories by `book_sort` then chronological; photos + follow-up thread embedded) + `loadBookStorytellers` for the picker. `api/book/photo`: admin-gated POST (multipart upload via service role + row insert) + GET (membership-gated read → short-lived signed URL → 307, same shape as the audio route). `book/actions.ts` (admin-gated, family-scoped): `reorderChapters`/`reorderStories`/`reorderPhotos`, `setPhotoCaption`, `removePhoto` (object-before-row). `BookEditor.tsx` (client, native HTML5 DnD — no library — with grip ⠿ + keyboard Move up/down fallback): drag chapters / stories-within-chapter / photos-within-story, attach/remove/caption photos. `/book` picker (single storyteller → redirect) + `/book/[id]` (admin editor / viewer read-only; derived book cover) + nav "Book" link. Cleanup (closes 5.2a deferral): `collectAnswerPhotoPaths`/`collectStorytellerPhotoPaths`/`removePhotoObjects` wired into `deleteStory` + `deleteStoryteller` so deletes erase photo objects too. **Deferred (next tasks, not dead-ended):** voice QR (7.2), print/export + "Order book" button (7.3), ES translation view (7.4). Verified: `tsc` + `npm run build` clean (`/book`, `/book/[id]`, `/api/book/photo` routes present); `sortChapters` pure harness 6/6; RLS `BEGIN…ROLLBACK` test (`supabase/tests/rls_book.test.sql`) PASSED (admin inserts photo + sets chapter order, viewer reads-not-writes, cross-family isolated); live round-trip vs the real bucket+DB 10/10 (upload → signed-URL serves exact bytes → collectors find story+follow-up photos → cross-family collect empty → answer delete cascades photo rows → removePhotoObjects erases the object), fixtures cleaned up.
- [x] 7.2 Voice QR codes that bundle (not just link) the audio — each in-book story now prints a **voice QR** that opens a public, login-free, never-expiring playback page. Scope (confirmed): the QR is the durable *link* half; the SPEC's "bundle the bytes so it survives independently" is the export's job (7.3 print / 7.6 ZIP), where the spec already places it (a QR physically can't carry an MP3). `lib/book/play-token.ts` — deterministic AES-GCM token over the answer id (reuses `STORYTELLER_TOKEN_SECRET`, domain-separated `:voice-qr`): same id → same token every reprint (stable QR), no DB row, fail-closed on missing secret, GCM tag rejects tampering. `api/p/audio` — public, token-gated audio (same private-bucket → short-lived signed-URL → 307 as `api/stories/audio`, but no login; a follow-up is served only if its parent is the token's story, so one token can't fish for other answers). `lib/book/public-story.ts` — service-role loader scoped to one story + its thread. `(storyteller)/p/[token]/page.tsx` — calm, large, elder-font play page in the storyteller's language; forged/dead token → calm screen (200), never a crash; `robots:noindex`. `lib/book/voice-qr.ts` — server-renders the QR as inline **SVG** (vector, print-ready for 7.3) encoding `${APP_BASE_URL}/p/<token>`; `book/VoiceQrTag.tsx` renders it + caption in both book views (`book.ts` now carries storyteller `language`); 7 en/es i18n strings; added `qrcode`. No schema change; no new secret (`STORYTELLER_TOKEN_SECRET` set in 2.2, `APP_BASE_URL` already on Cloudflare). Verified: `tsc` + `npm run build` clean (`/api/p/audio`, `/p/[token]` routes present); token unit harness 11/11 (deterministic, tamper/garbage/empty→null, no-secret→null); live HTTP e2e vs the linked DB + real Storage 16/16 (seeded story+follow-up+separate story, real audio objects → play page 200 shows question+follow-up, audio 307→signed URL, foreign follow-up 404, forged token 401/calm dead-link, QR token decodes back to the story id), cleanup confirmed DB back to original.
- [x] 7.3 Export / print order — server-rendered keepsake PDF via Cloudflare Browser Rendering (decided: server `book.pdf` now; POD vendor a future task). `lib/book/print-html.ts` `renderBookHtml` is the single print layout (cover → one chapter per page via `break-before:page` → story question/transcript/photo grid/inline vector voice-QR + localized caption/follow-up thread; `@page` Letter, Fraunces+Atkinson, escaped). Two consumers share it: `/book/[id]/print` (in-browser Save-as-PDF + fallback; photos via the cookie-authed `api/book/photo`), and `lib/book/pdf.ts` `renderBookPdf` (reusable for 7.6 — inlines photos as `data:` URIs, drives the `BROWSER` binding via `getCloudflareContext`; **fail-soft → null** when the binding is absent). `api/book/pdf?storyteller=<id>` streams `application/pdf` as a download (binding absent → 503 + print-view fallback; 502 render err; 404 cross-family). `/book/[id]` gains **Download PDF** + **Open print view** + an inert **Order a printed book · coming soon** placeholder (POD wired around 9.6). `wrangler.jsonc` `browser` binding (Workers Paid); `@cloudflare/puppeteer` dep; no migration/bucket/secret. **Audio bundling stays 7.6's job** (a PDF can't carry MP3; the QR is the print's audio access). Verified: `tsc` + `next build` + `opennextjs-cloudflare build` all clean (puppeteer bundles into the worker; `/api/book/pdf` + `/book/[id]/print` present); `renderBookHtml` pure-output harness 17/17. **Live PDF render verified post-deploy** (the `BROWSER` binding only exists on the CF runtime).
- [x] 7.4 Optional English translation view of Spanish transcripts — opt-in, admin-triggered, cached. Migration `0011` adds nullable `answers.transcript_en` (covers openers + follow-ups; applied to prod, types regenerated; inherits `ans_select`/`ans_write` — no new RLS). `lib/ai/translate.ts` `translateToEnglish` (server-only Anthropic call, prompt-cached, temp 0.2; faithful — preserves meaning/voice/names, handles ES↔EN code-switching, never sanitizes/summarizes; fail-soft). `stories/actions.ts` `translateStory` (admin-gated, family-scoped: selects opener + follow-up thread via `.or()`, translates only es rows with transcript + no cached translation, `Promise.all` fail-soft per row); `editTranscript` now clears `transcript_en` when the Spanish source changes (stale translation removed). Display: `/stories` shows an **English translation** `<details>` toggle under each es transcript (viewers see it once generated) + admin **Translate to English** button when untranslated; book reading surfaces (ReadOnlyBook + print/PDF via `print-html.ts`) render the English under each Spanish passage for **es books only** (BookEditor arrange tool unchanged). `lib/stories.ts` + `lib/book.ts` carry `lang`/`transcriptEn`. Verified: `tsc` + `next build` clean (no new Workers deps/bindings); live `translateToEnglish` faithful on Spanish + code-switch (names preserved); print/PDF render harness 7/7 (es-only, story+followup, null handling); BEGIN…ROLLBACK vs linked DB (or-filter selects opener+followup, untranslated filter matches, update caches both → translated_stored=2); column grant confirms `authenticated` UPDATE/SELECT on `transcript_en`. **Prod:** `ANTHROPIC_API_KEY` already set (3.2/3.4).
- [x] 7.5 Pricing model + public pricing page — single source of truth `src/lib/pricing.ts` (typed tiers/lifetime/add-ons/feature-matrix/FAQ/copy + `formatPrice`; each priced item carries `stripePriceId: null` so Phase 9.1 attaches IDs here, not a second declaration; `PRICING_UNCONFIRMED` flag + inline `TODO:`s on every COGS-pending value per §2/§3/§6) and `/pricing` page (`src/app/(marketing)/pricing/page.tsx`, server component rendering entirely from the module, brand tokens): hero → 3 tier cards (Keepsake+Book highlighted "Most popular") → Lifetime band (not a 4th card) → forever-access + voice-QR callout bands → add-ons table → feature matrix → FAQ → final CTA + page metadata. Prices = the spec's recommended numbers as a revisable v1 (decided 2026-06-05: don't ship as final until COGS confirmed — POD quote, per-storyteller cost, Lifetime guardrail). **No Stripe** (Phase 9): CTAs → `/login`; **no nav link added** (renders standalone until the 8.1 marketing shell). Reinforces both pillars (forever access on cancel, voice-QR book). **Coordinates with** 8.3 (this IS the pricing page) + 9.1/9.2 (consume `pricing.ts`) — keep in sync, don't hardcode prices twice. Verified: `tsc` + `npm run build` clean (`○ /pricing` prerendered static); live dev fetch 200 with all tiers/prices/callouts/matrix/FAQ/CTAs present.
- [x] 7.6 Full-archive export ("Download everything") — async, universal on every tier (ownership never paywalled/role-gated). Migration `0012_exports.sql` (applied to prod, types regenerated): `export_status` enum + `exports` job table (family/storyteller-scoped) + private `family-exports` bucket (no public policies). RLS: `exp_select`/`exp_insert = is_member_of` (ANY member reads + requests — a viewer can export), no member UPDATE (the worker mutates via service role). `lib/export/build.ts` = pure helpers (filename `<YYYY-MM-DD>_<Story-Title>` slug+dedup, per-story transcript `.txt`, `manifest.csv`, `README.html`). `lib/export/run.ts` (service role) `processExport(jobId)` atomically claims (queued→preparing), gathers the WHOLE archive, fetches audio bytes, renders `book.pdf` (reuses 7.3, null→omit + note), zips with **fflate** (store audio/PDF, deflate text), uploads, flips to `ready` (+7-day expiry), then a fail-soft EmailJS "ready" email; `runExportMaintenance` is the cron backstop (finish queued/stuck jobs + clean expired ZIPs), wired into `api/cron/scheduler`. `requestExport` action (ANY member; inserts via RLS, kicks off via `ctx.waitUntil`, cron backstops). `api/export/download` = login+RLS gated → short-lived signed URL → 307 (email links here, never a raw bearer URL). `ExportPanel.tsx` per-storyteller box (Idle→Preparing…→Ready, auto-refresh). `deleteStoryteller` now erases export ZIPs too. **Decisions:** one storyteller per export (Family "all" bundle deferred to Phase 9); **original audio format** (MP3 transcode needs ffmpeg, impossible on Workers); any-member access; 7-day retention; EmailJS now (Resend reserved for 10.1). **Known v1 limit:** fflate zips in memory → very large archives can hit the Worker memory ceiling (rate-limit + streaming-zip = future hardening, spec §7). Pricing §6 (matrix row + FAQ) already shipped in 7.5. Verified: `tsc` + `next build` + `opennextjs-cloudflare build` clean (fflate bundles); pure-helper harness 27/27; RLS `supabase/tests/rls_exports.test.sql` PASSED + negative control fired; live storage round-trip vs real DB + new bucket 14/14 (gather→pull real audio→zip→upload→sign→byte-identical download w/ friendly filename→unzip README/manifest/transcripts/audio→cleanup). book.pdf render + `ctx.waitUntil` ride the CF runtime → verify post-deploy (like 7.3). **Prod:** no new secrets (EmailJS/`APP_BASE_URL`/`BROWSER` already set).

## Phase 8 — Public marketing site & signup funnel
→ read: SPEC § Marketing, signup & billing · `myfamilyporch-website-brief.md` (full brief) · placeholder `src/app/(marketing)/page.tsx`
English only for v1; bilingual marketing is deferred. **Built in Next under
`src/app/(marketing)`**, reusing app brand tokens/fonts — the brief's
Astro/Cloudflare-Pages recommendation is **overridden** (decided 2026-06-01: one
codebase + one deploy, app stays on the apex). Confirm with the owner before
finalizing (brief §1, §13): pricing/tiers, prompt cadence, keepsake format, real
testimonials, real photos, email provider, final CTA destination.
- [x] 8.1 Marketing shell + SEO wrapper — single `(marketing)/layout.tsx` shell (skip-to-content → sticky `SiteHeader` → one `<main id="content">` → `SiteFooter`), nested under root so `(app)`/`(storyteller)` are untouched. `SiteHeader` = wordmark + desktop nav + "Get started" CTA + translucent backdrop-blur; `MobileNav` (the only JS island) = hamburger closing on route-change/Esc with `aria-expanded`/`aria-controls`. `SiteFooter` = wordmark/tagline + Product/Company/Help/Legal columns + "never sold" privacy line. Shared `Container`/`Section` spacing tokens + `nav.ts` shared model (CTA→`/login`). `lib/seo.ts` `pageMeta({title,description,path})` → canonical + OpenGraph + Twitter `summary_large_image`; root layout sets `metadataBase` + defaults. Home/pricing/privacy/terms refactored into the shell (single `<main>`, no duplicate headers) + migrated to `pageMeta`. Nav anchors (`/#how-it-works`·`/#stories`·`/#faq`) fill in 8.2; `/pricing` live. Reuses the app blue Fraunces/Inter tokens (brief Astro/terracotta direction overridden per Phase 8 note). `SITE_URL=myfamilyporch.net` + OG `/og.png` carry `// TODO`s (confirm domain; 8.9 asset). Verified: `tsc` clean; `next build` clean (`/`,`/pricing`,`/privacy`,`/terms` all `○` static); live dev fetch — every page renders exactly one `<main>` (no nesting) with header/footer/skip/nav present; SEO live-checked (no doubled title suffix, absolute canonical + og:image, twitter card). **Known:** `/og.png` 404s until 8.9.
- [x] 8.2 Landing page sections — expanded `(marketing)/page.tsx` from hero-only into the full home: hero (secondary CTA → `/#how-it-works`) · the-problem-gently · how-it-works (3 numbered steps) · why-voice-matters · what-you-get (4 keepsake cards) · social proof (3 testimonials, `// TODO: real quotes`) · pricing preview (3-tier teaser from `lib/pricing` `TIERS`, recommended highlighted → `/pricing`) · FAQ (native `<details>` accordion from `lib/pricing` `FAQ`, accessible + zero JS) · final CTA band. **Server-rendered, no new JS island** — `/` stays `○` static (178 B First Load, unchanged). Section ids (`#how-it-works`/`#what-you-get`/`#stories`/`#faq`) match `nav.ts` + `SiteFooter` anchors exactly (no dead links). Reuses `Section`/`Container`, `lib/pricing` (`TIERS`/`FAQ`/`formatPrice`), `.card`/`.btn-*`/`.chip` tokens, `PRIMARY_CTA`. **Porch motif** (brief §5) threaded as a sound-wave/railing accent (why-voice) + a warm porch-light glow on the dark final-CTA band. Left `// TODO: confirm keepsake format` (brief §13). Verified: `tsc` clean; `next build` clean (`/` `○` static, 178 B — no JS island); live dev fetch — exactly one `<h1>`, all four anchors present, all 8 section headings render, FAQ `<details>` present.
- [x] 8.3 Pricing page — added the missing **gift option** to the existing `/pricing` page (tier cards, recommended highlight, and the one-time book add-on shipped in 7.5/8.1). New `giftCallout` (h2/body/cta) in `lib/pricing.ts` keeps gift copy in the single source of truth; pricing page renders a 🎁 **gift band** after the Lifetime band (subscribe → buy once → give as a gift), framed for the brief's gift-giver audience (birthdays / Mother's & Father's Day / holidays / milestones). Gift CTA → `GIFT_HREF` = `/login` for now with `// TODO: repoint to /gift in 8.7` (no dead button per the marketing rule; the `/gift` flow is 8.7). No schema/deps/secrets; no new JS island. Verified: `tsc` + `next build` clean (`/pricing` stays `○` static, 178 B unchanged); live dev fetch `/pricing` 200 with exactly one `<h1>` and the gift band heading/body/CTA/mark all rendering. (brief §4.7)
- [x] 8.4 FAQ + legal/contact pages — added dedicated `/faq` (grouped accordion: Getting started · Privacy & security · Your keepsake · Billing & ownership) and `/contact` (support email + topic cards incl. privacy/deletion + SMS STOP/HELP note), both `○` static / no JS island. `lib/pricing` gains `FAQ_GROUPS` (single source) with the homepage teaser `FAQ` re-derived from `featured` items (no drift). **Privacy** got a prominent "Your voice recordings" box spelling out the three required points — *how stored* (private, encrypted in transit, family-isolated), *who can access* (signed-in family after access check; storyteller via private link; vendors only on our behalf), *never sold* — plus retention/deletion. **Terms** gained "Who owns the content" + "AI and voice processing"; A2P/CTIA SMS section preserved intact (STOP/HELP + message rates verified). Nav FAQ→`/faq`; footer Help→FAQ `/faq` + Contact `/contact`; homepage teaser → "Read the full FAQ →". Verified: `tsc` + `next build` clean (`/faq`,`/contact` `○` static 187 B); live dev fetch all 200 with one `<h1>`/`<main>`, `/faq` 12 items + teaser 6, cross-links resolve, legal language asserted present. (brief §4, §10)
- [x] 8.5 Signup CTA → stable `/signup` seam → (today) `/login` → existing `create_family` onboarding → dashboard. **Stripe Checkout deferred to 9.2** (owner-confirmed scope: pricing/products in 9.1 don't exist yet). New `src/app/(auth)/signup/route.ts` = the single funnel seam the app owns: `GET /signup` 307→`/login`, preserving a **safe relative** `?next` and dropping off-site values; `TODO(9.2)` documents the swap to a Stripe Checkout session (success_url returns into `/auth/callback` → unchanged onboarding), so marketing never changes again. CTA target centralized in `_components/nav.ts` `PRIMARY_CTA.href` (`/login`→`/signup`) — all four render sites (header, MobileNav, hero, final-CTA band) already consume it, so no scattered/hardcoded URLs. Kept **same-origin relative** on purpose (marketing+app = one apex deploy == `APP_BASE_URL`): resolves against the live origin AND keeps marketing pages statically rendered (no runtime env read); flip the one constant to `${APP_BASE_URL}/signup` if origins ever split. Verified: `tsc` clean; `next build` clean (`/signup` `ƒ` route handler; `/`,`/pricing`,`/faq`,`/contact`,`/privacy`,`/terms` stay `○` static — no regression); live dev — `/` CTA hrefs all `/signup`, `GET /signup`→307 `/login`, `?next=/invite/abc` preserved, `?next=https://evil.com` dropped. No deps/schema/secrets.
- [x] 8.6 `/how-it-works` (expanded), `/about` (brand story — "why the porch"), and a custom on-brand 404 (brief §4). Three server-rendered, `○`-static pages, no JS island / deps / schema / secrets, reusing the 8.1 shell (`Section`/`Container`/`pageMeta`), brand tokens + porch sound-wave/porch-light motif. **`/how-it-works`** — expanded walkthrough: intro hero → 3 steps with real detail → "gentle for elders" trio → what-you-get recap (→ home `#what-you-get`) → privacy reassurance (→ `/privacy`) → CTA; `// TODO: confirm cadence` kept, copy stays vague on cadence. **`/about`** — brand story (chose `/about` over `/stories` to avoid colliding with the home `#stories` testimonials anchor): "why the porch" narrative → mission ("don't let the stories disappear") → 3 values (warm/private/yours) → who-we-are (Technology Leadership Group, LLC, matches footer disclosure) → CTA. **Custom 404** — global `src/app/not-found.tsx` rebuilds the marketing shell (skip link → `SiteHeader` → `main` → `SiteFooter`) since a root not-found renders only in the root layout; "wandered off the porch" copy + Home/CTA + 5 helpful links; `robots:noindex`. Wiring (no dead links): `nav.ts` "How it works" → `/how-it-works`; `SiteFooter` Product "How it works" → `/how-it-works`, Company "Our story" → `/about` (hero `/#how-it-works` anchor still scrolls within home, intended). Verified: `tsc` + `next build` clean (`/how-it-works`,`/about`,`/_not-found` all `○` static; `/`,`/pricing`,`/faq`,`/contact` unchanged — no regression); live dev — both pages 200 with exactly one `<h1>`/`<main>`, bad URL → **404 status** with on-brand header/footer + porch copy + helpful links, home renders updated header→`/how-it-works` + footer→`/about`.
- [x] 8.7 `/gift` gifting landing page (gifting is a primary use case) → Gift CTA into the gift flow — new server-rendered, `○`-static `(marketing)/gift/page.tsx` (no JS island/deps/schema/secrets) reusing the 8.1 shell (`Section`/`Container`/`pageMeta`) + porch sound-wave/porch-light motif: hero ("The gift that keeps their voice") → why-it's-meaningful → how-gifting-works (3 giver-framed steps) → "perfect for" occasions grid (birthday/Mother's & Father's Day/holidays/milestones/just-because) → what-they-receive (voice/written stories/photos/book-with-voice-QR) → no-tech-worries reassurance (→ `/privacy`) → final CTA band. **Scope:** the gift *purchase/redemption* is 9.7, so the page's buy CTAs route to the existing `/signup` seam via one `GIFT_BUY_HREF` const + `// TODO(9.7)` (no dead button); copy stays vague on redemption (`// TODO: confirm gift-flow details`, brief §13). **Wiring (single source, no stale anchors):** `nav.ts` new exported `GIFT_HREF="/gift"`; pricing page drops its local `GIFT_HREF="/login"` and imports the shared one (gift band → `/gift`); `SiteFooter` "Gift a porch" `/#gift` → `/gift`. Primary header nav unchanged (brief §3 set; gift reached via footer + pricing band). Verified: `tsc`/`next build` clean (`/gift` `○` static, other static pages no regression); live dev — `/gift` 200 with exactly one `<h1>`/`<main>`, buy CTAs → `/signup`, pricing has 2 `/gift` links + 0 stale `/#gift`, home footer 1 `/gift` + 0 stale. (brief §1, §4, §7; gift purchase itself is 9.7)
- [x] 8.8 Email capture for not-ready visitors (brief §7) — new `"use client"` island `EmailCapture.tsx` (the marketing site's 2nd JS island after MobileNav): name + email + off-screen honeypot (`website`, `tabIndex=-1`, `aria-hidden`); idle→loading ("Sending…", disabled)→success (form replaced by warm thank-you card)→error (inline `role=alert`/`aria-live`, form stays to retry); POSTs JSON to same-origin relative `/api/subscribe` (marketing+app = one apex deploy → no CORS / no `APP_BASE_URL`); reuses `.input`/`.btn-primary`/`.card` tokens, large tap targets for the older audience. New Worker route `api/subscribe/route.ts` (POST): honeypot non-empty → silent `{ok:true}` (don't tip off bots); **zod** server-side validation (name 1–100, valid email) → `400 {error}`; valid → `console.log` lead (always in Worker logs) + best-effort EmailJS notification (reuses `lib/email/emailjs` `sendEmail`, HTML-escaped) to `LEAD_NOTIFY_EMAIL` (falls back to `support@myfamilyporch.net`), **fail-soft** so a delivery error never fails the visitor; `// TODO` left for a real mailing-list provider (ConvertKit/Mailchimp/Resend Audiences) — none configured yet. **No migration/bucket/blocking secret, no DB row** (a lead is pre-account, no `family_id` → not in the RLS-scoped schema). Home (`page.tsx`) gains a "Not ready yet?" `<NotReady>` section between FAQ and the Final CTA (only render site). New `.dev.vars.example` (brief deliverable, was missing): full key template w/ placeholders, no real secrets (git-trackable — not caught by the `.dev.vars` ignore) + `LEAD_NOTIFY_EMAIL` added to both `.dev.vars` files. English-only (Phase 8). Verified: `tsc` + `next build` clean (`/api/subscribe` `ƒ`; `/` stays `○` static, 1.34 kB First Load — island hydrates client-side, page still prerendered); live dev POST round-trip — VALID→200 `{ok:true}` (+ server-logged lead), bad email→400, empty name→400, **honeypot filled→200 with no lead log** (silent), malformed JSON→400; home renders the section + honeypot field + email input + submit label with exactly one `<h1>`.
- [ ] 8.9 Brand assets: wordmark/logo SVG + porch mark, favicon set, OG/social image (1200×630), hero/section illustrations + phone & keepsake mockups (brief §6)
- [x] 8.10 SEO files + structured data — added `src/app/sitemap.ts` (`/sitemap.xml`, the 9 public marketing routes only — private app/auth/storyteller surfaces excluded — built from `SITE_URL` with per-page changefreq/priority) and `src/app/robots.ts` (`/robots.txt`: allow `/`, disallow `/api/`·dashboard·settings·`/s/`·`/p/`·`/login`·`/signup`·etc., + Sitemap/Host). JSON-LD via new pure builders `src/lib/jsonld.ts` (read the **existing single sources** so structured data can't drift: `organizationLd` Organization+WebSite from `lib/seo`, `faqPageLd` FAQPage from `FAQ_GROUPS`, `pricingProductLd` Product+Offers from `TIERS`+`LIFETIME`) rendered by a tiny server `JsonLd.tsx` (no JS island) — Organization on home, FAQPage on `/faq`, Product/Offers on `/pricing`. **Canonical URLs already done** (every page's `pageMeta` emits `alternates.canonical`, 8.1) and **alt text already satisfied** (marketing uses no `<img>`; visuals are decorative inline SVG correctly `aria-hidden`; `/og.png` carries `alt`) — both re-verified unchanged. No schema/migration/bucket/secret/deps/JS island. Verified: `tsc` + `next build` clean (`/sitemap.xml` + `/robots.txt` prerender `○` static; `/`,`/faq`,`/pricing` + all marketing pages stay `○`, `/` First Load unchanged 1.34 kB); live dev — robots.txt allow/disallow + Sitemap/Host correct, sitemap.xml valid w/ all 9 absolute URLs, each page's JSON-LD valid with its root type (`Organization`/`FAQPage`/`Product`) appearing **exactly once** (higher raw grep counts were dev-mode RSC flight echoes), and **no regression** (each page still 1 `<h1>` + its canonical link). **TODO(domain):** `SITE_URL` still `// TODO: confirm myfamilyporch.net` (single source). (brief §8)
- [x] 8.11 Performance & a11y pass — pure CSS/markup hardening, no schema/deps/secrets/routes/JS-bundle growth. **globals.css** (product-wide): global `:focus-visible` (2px brand outline + offset, was relying on the clipped browser default — WCAG 2.4.7) + `@media (prefers-reduced-motion: reduce)` collapsing transition/animation durations & forcing `scroll-behavior:auto` (WCAG 2.3.3). **layout.tsx**: `viewport` export w/ `themeColor` (Next already injects width=device-width). **Contrast** across the whole marketing surface (page/not-found/SiteFooter/EmailCapture/pricing/about/how-it-works/gift/privacy/terms/contact): folded the failing muted tokens up to `text-ink/65` (≈5.0:1, passes AA-normal, no hierarchy inversion since body is also /65) — `/45`≈2.7, `/50`≈3.3, `/55`≈3.7, `/60`≈4.4 all failed; matrix "not-included" dash `/25`≈1.8 → `/55`≈3.7 for non-text contrast (1.4.11); left `text-white/70` on dark bands (≈8:1, already AAA). Verified: `tsc` + `npm run build` clean, every marketing route stays `○` static (home First-Load 1.34 kB unchanged → no JS regression), sRGB contrast math per token, grep confirms no failing tokens remain, responsive 320–1440 (no overflow). **Lighthouse 90+ verify post-deploy** (no headless Chrome in the sandbox — owner-accepted, like 7.3/7.6 runtime-only checks). (brief §9, §12)

## Phase 9 — Subscriptions & billing
→ read: SPEC § Marketing, signup & billing · SPEC § Multi-tenancy & roles
Pay-during-signup; subscription (tiered) + one-time book add-on; owner holds billing.
- [ ] 9.1 Stripe products/prices: subscription tiers + one-time book add-on + gift SKU; env + webhook secret on Cloudflare (exact tiers/pricing pending owner confirmation — brief §1)
- [ ] 9.2 Checkout session from pricing/signup CTA (creates customer, ties to `family_id`/owner)
- [ ] 9.3 Webhook handler → persist subscription + entitlement state on the family (migration)
- [ ] 9.4 Billing portal (owner-only: upgrade/downgrade/cancel/update card)
- [ ] 9.5 Feature gating from entitlement state (block dashboard/interviews when unpaid) + optional trial
- [ ] 9.6 One-time book add-on checkout wired to the keepsake export (Phase 7)
- [ ] 9.7 Gift purchase + redemption: one-time gift checkout (from `/gift`, 8.7) mints a redeemable code/link; recipient redeems → runs `create_family` onboarding → becomes owner; gift payment tied to the redeeming family (brief §1, §4, §7)

## Phase 10 — Launch hardening
- [ ] 10.1 Resend transactional + billing email (migrate off EmailJS where appropriate)
- [ ] 10.2 Rate limits, audit logging, media backups
